env: "local" # local, dev, prod

storage_path: "./storage/storage.db"

http_server:
  address: "0.0.0.0:8085"
  timeout: 4s
  idle_timeout: 60s
  shutdown_timeout: 10s
  user: "admin"
  password: "password"
